import 'package:flutter/material.dart';

class FidelityCard extends StatelessWidget {

  final int? points;

  FidelityCard({Key? key, required this.points}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();

  }
}
